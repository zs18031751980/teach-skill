# 独立试讲输入

这些材料只用于维护时实际试讲，不由教学入口加载。2026-09-13 将每个请求和新主文件路径交给无前文的独立代理，代理读取当前主文件及命中参考后生成同目录答案文件。没有向代理提供期望答案或已知问题。

## knowledge.md

Python 的 `sorted(items, reverse=True)` 里 `reverse=True` 到底改变什么？我只是不明白这个参数，不需要整套排序课程。

## code.md

请逐行解释下面 Python 代码，语法和逻辑都讲清楚，说明最终 data 和 backup 的值：

```python
data = [1, 2]
backup = data
# 在原列表末尾追加元素
backup.append(3)
total = sum(data)
print(total)
```

原始行号 1—6，行号不属于代码。

## algorithm.md

给定整数数组 nums 和整数 target，返回两个不同下标，使对应数之和等于 target；允许重复数值，无解返回 []，多个解返回任一组。请解释思路、给完整 Python 函数 `two_sum(nums, target)`，并分析时间空间复杂度。

## document.md

把下面这份短材料按 L2 概括成 Markdown 文件，保留全部信息：

> # 缓存试验记录
> 在本次测试的配置下，方案 B 的平均响应时间由基准方案 A 的 100 ms 降至 80 ms。该结果只来自 1000 次只读请求。两组方案使用相同机器，测试期间没有写入请求。错误率从 3% 降至 1%，即减少 2 个百分点。上述结果尚不能证明性能变化由缓存单独造成。下一步计划加入写入请求，但尚未执行。
